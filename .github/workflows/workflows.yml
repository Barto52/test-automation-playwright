name: Playwright Tests

on:
    push:
        branches: [main, master]
    pull_request:
        branches: [main, master]

jobs:
    test:
        runs-on: ubuntu-latest
        container:
            image: docker:latest

        steps:
            - uses: actions/checkout@v4

            - name: Run Docker container
              run: |
                  docker run -p 3000:3000 -d jaktestowac/gad:2.7.9

            - name: Wait for the app to start
              run: |
                  until curl -s http://localhost:3000; do
                    echo "Waiting for the app to start on localhost:3000..."
                    sleep 5
                  done

            - name: Run Playwright tests
              run: |
                  npx playwright test

            - name: Tear down Docker container
              if: always()
              run: |
                  docker ps -q | xargs docker stop  # Zatrzymanie kontenera
                  docker ps -q | xargs docker rm    # Usunięcie kontenera

            - name: Upload Playwright test reports
              if: ${{ !cancelled() }}
              uses: actions/upload-artifact@v4
              with:
                  name: playwright-report
                  path: playwright-report/
                  retention-days: 30
